{#
<\?=\$([^\?]+)\?>
{{ $1 }}
#}
<!DOCTYPE html>
<html>
	<head>
		<title>{{ constant('SITE_TITLE') }} - {{ title }}</title>
		<link rel="stylesheet" href="{{ constant('WEB_ROOT') }}/style.php/style.scss" />
		<link rel="stylesheet" href="{{ constant('WEB_ROOT') }}/jquery.tagit.css" />
		<link rel="stylesheet" href="{{ constant('WEB_ROOT') }}/jquery-ui-1.10.0.custom.min.css" />
		<script src="{{ constant('WEB_ROOT') }}/js/jquery-1.9.0.js" type="text/javascript"></script>
		<script src="{{ constant('WEB_ROOT') }}/js/jquery-ui-1.10.0.custom.min.js" type="text/javascript"></script>
		<script src="{{ constant('WEB_ROOT') }}/js/tag-it.min.js" type="text/javascript"></script>
		{{ head|raw }}
	</head>
	<body>
		<section id="wrap">
			<section id="header">
				<h1>{{ constant('SITE_TITLE') }}</h1>
				<ul id="plinks">
				{% for dat in links %}
					<li id="link-{{ loop.index }}">
						<a href="{{ dat.getURL()  }}"><img src="{{ dat.image }}" alt="{{ dat.desc }}" /></a>
					</li>
				{% endfor %}
				</ul>
				{% if session != false %}
				<div id="sessinfo">
					Welcome back, {{ session.ckey }} ({{ session.role }})!
				</div>
				{% endif %}
			</section>
			<section id="content">
				{{ body|raw }}
			</section>
		</section>
		<section id="footer">
			<ul>
				<li class="first">vgstation-web</li>
				<li>Available under the MIT Open Source License.</li>
			</ul>
		</section>
	</body>
</html>
